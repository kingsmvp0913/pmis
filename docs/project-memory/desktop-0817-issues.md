---
name: desktop-0817-issues
description: 桌面 0817 資料夾回報的 14 個問題:分類、根因與逐項狀態(施工日誌辨識 ×11、監造報表產出 ×3)——全部處理完
metadata: 
  node_type: memory
  type: project
  modified: 2026-08-19T02:51:43.998Z
---

使用者 2026-08-19 交來桌面 `OneDrive/Desktop/0817/`,兩個子資料夾、47 個檔案。
**問題描述全在截圖裡,沒有文字清單**——先把 17 張 jpg 讀完再動手,不要從檔名猜。
✅ 12 個 commit(`13c0d98`→`36628e3`)全部處理完,1594 綠(含真實 Excel COM)。

## 分流的兩支工具(可重用)

- **對不出檔案屬於哪一家**:把每個檔跑過 `data/vendor-parsers/` 的**全部**讀取器,
  按「讀到幾列 / 缺項次幾列 / 缺契約數量幾列」排序。缺欄率的形狀直接指出是哪一支
  有 bug(明禮:禾結 缺項次 30、缺數量 21;陳宏鈞 全 0 → 但正確答案是禾結,
  **不要照分數挑,要看截圖裡的工程名稱**)。⚠️ 跳過 `1.pmisparser.js`(它自己
  require 檔型檔,會 MODULE_NOT_FOUND)。
- **哪些常駐報表漏修**:`data/reports/project_*/監造報表.xlsm` 的 mtime
  對 `git log -S <關鍵字>` 的日期。這一輪就是這樣定位到「刪列只掛在 SP2」。

## 修好的(逐項)

### D5 兩個假硬錯 — `13c0d98` `28b8eba`
1. **項次對應**:E1 早就會依項目名稱把日誌的「二/三/四/五/六」或接續阿拉伯數字
   (元長鋪面是 22~26)對回契約的「貳~陸」,soft 放行。但 D5 自己另外建
   `有 = new Set(dailyRows.map(r => String(r.項次)))`,**既沒 normNo 也沒吃名稱
   對應**,於是同一批費用項目每個月末都判成缺項。元長鋪面、橋頭許厝分校、鹿場全中。
   → 逐列迴圈裡記 `當日契約項次`(對得上契約就記契約的編號),D5 改用它。
2. **月中上傳**:D5 拿「這批的最後一天」當月結點,而廠商 7 月的檔常多印幾天
   (元長鋪面到 8/7)或整月預先列印(橋頭到 9/12)。→ 只驗**已經收完**的月份:
   那天就是該月最後一天,或後面還有別的月份的資料。

⚠️ **這類「同一件事在兩處各判一次」在 daily-log-validate 裡不只一處**——
E1 對應成功時補記 `seenItemNos` 的註解就是前一次踩到。加規則前先找既有的
正規化/對應層。

### 監造報表印出一整排 `#N/A`、進度 9092.78% — `b9edd4d`
⛔ **不是 `itemsToOperations` 算錯**——它產生的 deleteRows 完全正確。真因是
**刪列 2026-08-11 才加(`56b4a54`),而且只掛在 SP2 寫價目表那條路徑**。承辦人
日常只上傳日誌(SP3)不會重跑 SP2,8/11 之前建的常駐報表就永遠留著範本自帶的
五列費用公式(`ROUND(SUM($F$2:$F$32)*7%,0)`)。那些列 A~D 空、E 有公式 →
`IF(價目表!A2="",NA(),…)` 吐 #N/A,金額卻看起來合理,再汙染合計與百分比。
→ 抽成 `contract-items.resizeOperations(items, 現有列數)`,SP3 寫入前也跑一次;
`itemRowCounts` 移到 `report-workbook.js`(contract-items.js 刻意純函式)。

⚠️ **測 SP3 路由時工作簿會被前面的測試汙染**:`daily-log-routes.test.js` 全檔
共用一個 `PMIS_DATA_DIR`,而 pg-mem 每支測試重新發 id,project_1 的報表早被
`fillTemplate` mock 寫成純文字 `'xlsm'`。要測列數得先 `copyFileSync(TEMPLATE_PATH,…)`。

### 禾結:長名稱折行,整列被拆成兩列 — `c0bbd9f`
長名稱折成兩到三行,而**項次與數值不一定落在同一行**:有時數值在下一行(項次 3)、
有時項次在上一行(項次 27),契約數量甚至自己單獨一行。一行當一列 → 每天多出沒有
項次的假列(A4)、真列名稱只剩半截(E3)、漏掉契約數量(A7)。明禮 1050 列裡
30 個 A4、21 個 A7 → 修後都是 0。
⚠️ **不可以照行距併**:項次 3 的數量行離項次 4 的名稱首行只有 7.7,而同一列各行
是 5.0~7.7。改成把沒有項次的行歸給**最近的項次行**(5.4 vs 12.9 分得開)。
📌 土庫立德的表頭順序與缺欄型態一模一樣(同為禾結格式),由同一修正涵蓋。

### 嘉原:續頁(第 2 頁)整頁沒讀 — `a92f698`
有些天在日誌頁後面多印一頁「第 2 頁,共 2 頁」:同一個表單編號、**沒有填表日期**,
帶著全部項次外加日誌頁根本沒有的單價與金額。它既不是「有日期的一天」也讀不出
明細(表頭欄位不同),整頁被丟掉 → 新光 7/31 只剩 4 項,D5 判「缺 10 項」。
→ 續頁是嚴格超集,有就整批取代。
⚠️ 順手修掉一個順序假設:表單編號的值**不是「陣列裡的下一個 item」**(續頁實測
是「合約項次」),改取同一帶右邊最近的那個。

### 錯誤訊息三件 — `8cb75fe` `919dc0a` `0244324`
- 讀取器讀不動 → 原本回無差別 500「請稍後重試」,而讀取器的錯**重試一萬次也不會
  成功**。改 400 + 讀取器原訊息 + 檔名;提到文字層/掃描件時指路「辨識掃描件」。
- **Express 沒有 error handler** → multer 之類的錯掉進預設處理器回 HTML,
  前端讀不到 `error`,畫面只剩「HTTP 500」(東榮兩檔同傳)。已補 JSON handler。
- **有文字層的檔被拿去 OCR**:僑美/四湖那幾份讀取器讀得完美,承辦人卻按了
  「辨識掃描件」,等好幾分鐘換來 0 天。掃描路徑改成先試文字層,讀得動就當場指路。
  同時修 `[]` 是 truthy → 空陣列走「可預填」,給出一顆按了會失敗的寫入按鈕。

### 龍井掃描件 — `919dc0a`
銘佑那個格式沒有文字層,讀取器唯一入口是 `ctx.filetypes.extractItemsOcr`;
`scanDays` 只換掉 `extractItems`,於是永遠回「缺少注入的 filetypes.extractItemsOcr」。
→ 注入**剛算好的那份 pages**(不是再跑一次 OCR,一次要數十秒)。修後 3 天 × 35 列,
48.6s。

### E6 單價容差 — `5a30669`
元長廁所:廠商 Excel 的營業稅單價是算出來的 **51967.3**(印出來顯示 51,967),
契約表存整數 51967。差 0.3 被 0.01 容差判成硬錯,21 天每天一個。
→ 單價改元級容差(0.5);**數量不比照辦理**,0.3 M2 是真的差。
同時把訊息裡的推導金額四捨五入到分(原本印 `72001.95000000001`,像系統壞掉)。

## 承辦人的裁決:多讀到的那幾天照收

元長鋪面那個「7 月」的檔實際含 7/09~**8/07** 共 30 天。他截圖寫過兩句互相打架的話
(「日期只需讀取到 7/31」vs「數據只要讀取到上傳日當天或前就可以」),2026-08-19
當面確認 → **照現況,8/7 讀進來**。理由:那是檔案裡真的有的資料,8 月的日誌上傳時
同一批日期會被覆蓋。他抱怨的實害只有 D5 拿月中的 8/7 當月結點,那個已修(`28b8eba`)。

⚠️ 所以**不要**再去做「依檔名月份截斷」或「擋掉超出當月的日期」——那是被否決的方案。

## ⛔ 不是程式問題(已查證,別再挖)

- **巨集 `Button3_Click` 執行不了**:產出檔的 `vbaProject.bin` 與按鈕綁定
  (`[0]!工作表6.Button3_Click`)完好,`Button1~3_Click` 三支都在。擋掉的是
  Windows 給下載檔案加的 `Zone.Identifier`(**實測 5 份全帶 `ZoneId=3`**,
  查法:`Get-Content <檔> -Stream Zone.Identifier`)。
  ⛔ **更正**:我先說「程式端修不掉」是錯的。標記是**瀏覽器**寫的、不是檔案內容,
  而 PMIS 是用啟動.bat 在本機跑的(伺服器與瀏覽器同一台)——由伺服器直接
  `copyFileSync` 到 `data/output/proj_<id>/` 就完全沒有標記(已實測驗證)。
  `784a3a6` 把「下載監造報表」改成先走 `POST report/save-local` 並開啟資料夾;
  非本機連線(看 `req.socket.remoteAddress` 與 `X-Forwarded-For`)要**擋下**
  ——那樣存的是伺服器那台的硬碟——擋下後才退回瀏覽器下載並教他解除封鎖。
- **明禮 B4**:廠商把「本日累計金額」當**當日金額**在填(7/03 比 7/02 少、
  7/03 與 7/04 一模一樣)。已登記進 `docs/來源資料缺陷.md`,B4 報得對。
- **東榮第二聯「找不到明德版面」**:在這台機器上明德讀得動(16 天 176 列)。
  推測是使用者上次啟動早於 2026-08-15 那批讀取器修正,裝在 `data/vendor-parsers/`
  的還是舊版。**動作是重開一次,不是手動重裝**(見下)。

⛔ **更正(使用者 2026-08-19 指出)**:我先前說「repo 的 sample 修好了不等於產線
更新了,要重新安裝讀取器」是**錯的**。實際流程是全自動的:

    啟動.bat → git pull --ff-only(node 起來之前)
             → app/scripts/start.js → runStartupOnboarding()
             → 掃 server/parsers/vendors/samples/ → registry.install()

`registry.install()` 的註解寫得很白:「驗證通過 → 搬到正式位置(**覆蓋舊版**)」,
沒有「裝過就跳過」的分支。所以**改完 sample 只要重開一次就生效**,
`data/vendor-parsers/` 不進 git 這件事不影響。
⚠️ 但 selfTest 沒過的讀取器會被 onboarding 略過(只印 warn),改讀取器後
selfTest 一定要保持綠的,否則產線會靜靜地繼續用舊版。

相關:[[supervision-report-pipeline]] [[sp3-daily-log-findings]]
[[xlsm-excel-com-findings]] [[source-data-defect-log]] [[commit-then-memory]]
[[vendor-parser-coverage-plan]]
